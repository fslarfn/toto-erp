import ModulePlaceholder from "@/components/layout/ModulePlaceholder";

export default function AlucurvWorkspacePage() {
    return (
        <ModulePlaceholder
            title="Workspace Alucurv"
            description="Fondasi integrasi (tabel database & akses workspace) sudah aktif. Modul operasional Alucurv (Order, Invoice, Surat Jalan, Stok, HPP, Pengadaan, Kas/Laporan) akan dipindahkan ke sini secara bertahap dari prototipe di alucurv-legacy/."
        />
    );
}
